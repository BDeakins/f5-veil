# Architecture (Placeholder)

Authoritative architecture documentation will be written by ETCH during the
documentation phase of the FORGE workflow. This file is a placeholder so the
`docs/` directory is committed alongside the rest of the scaffold.

Topics this document will cover:

- Two-pass obfuscation pipeline (discover → substitute).
- Placeholder taxonomy and naming conventions.
- IP placeholder mapping (RFC 5737 / RFC 3849).
- Answer file format and AES-256-GCM encryption scheme.
- Leak detector heuristics and `--strict` semantics.
- iRule (Tcl) body parsing strategy.
- Cross-reference integrity guarantees.
- Future persistent cross-run identifier map (v2.0).
